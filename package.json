{
  "name": "ethdenver-badge-subgraph",
  "license": "GPL-3.0-only",
  "scripts": {
    "codegen": "graph codegen",
    "build": "npx graph codegen && graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ scaredibis/tracer-pools-v2-arbitrum-rinkeby",
    "create-local": "graph create --node http://localhost:8020/ ethdenver-badge-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ ethdenver-badge-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ethdenver-badge-subgraph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.26.0",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.22.0"
  },
  "version": "0.0.0"
}
